# Browser requests remain same-origin in development and production.
VITE_API_BASE_PATH=/api/v1

# Vite development proxy target. This value is not bundled into browser code.
MUSE_DEV_API_ORIGIN=http://127.0.0.1:8000
